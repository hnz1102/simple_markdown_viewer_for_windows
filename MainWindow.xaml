<Window x:Class="markdown_viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:markdown_viewer"
        mc:Ignorable="d"
        Title="MarkdownViewer" Height="300" Width="400"
        WindowState="Normal"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="ここにマークダウンファイルをドラッグドロップしてください。"
                   HorizontalAlignment="Center" 
                   FontSize="16" Foreground="Gray"/>
            <TextBlock Text="Drag and drop markdown files here."
                   HorizontalAlignment="Center" Margin="0,10,0,0"
                   FontSize="16" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Window>
